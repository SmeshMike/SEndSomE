
const user = require('./user.map');
const chat = require('./chat.map');
const message = require('./message.map');
const connection = require('./connection.map');

module.exports = {
    user,
    chat,
    message,
    connection
}